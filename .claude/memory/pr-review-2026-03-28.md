# PR Review Session — 2026-03-28

## PRs Reviewed

### PR #25 — Deliberation Schema (FPL-30)
- **Branch**: me/fpl-30-deliberation-schema-and-working-memory
- **Status**: Review complete, no blocking issues
- **Migration**: 0010_deliberation.sql (correct numbering)
- **Key concern**: Phase output key semantics — output stored under new phase name, not the phase that produced it. Docstring is misleading. Design decision for FPL-36.

### PR #26 — Intent Queue & Evaluator (FPL-32)
- **Branch**: me/fpl-32-intent-queue-and-evaluator
- **Status**: Review complete — blocked on conversation module rebase
- **Migration**: Renumbered 0008 → 0010 (collision fix applied)
- **Fixes applied** (commit 933d11b):
  - Migration renumber 0008 → 0010
  - `updated_at NOT NULL DEFAULT now()`
  - Narrowed `contextlib.suppress(Exception)` to `except ImportError`
- **Blocker**: conversation.py was refactored to conversation/engine.py on main. PR targets stale file structure.
- **Prior fix commit** (7d13b0a by author) already addressed: atomic fetch+start, division-by-zero, date scanner dedup, typed publish_intent, stop timeout.

### PR #30 — Budget Controls & Token Tracking (FPL-34)
- **Branch**: me/fpl-34-budget-controls-and-token-tracking
- **Status**: Review complete, no blocking issues remaining
- **Migration**: Renumbered 0011 → 0012 (collision with 0011_action_log.sql from #29)
- **Fixes applied** (commit d2b4557):
  - Migration renumber 0011 → 0012
  - Moved `BudgetWarning` from durable section to ephemeral section in events.py
- **Non-blocking concerns raised**:
  - Redundant DB queries: `_check_warnings` re-queries daily+session totals after every insert (5 DB ops per tool-loop iteration)
  - Unbounded `_warned_bands` dict: session keys never pruned, slow memory leak over months
  - Rolling 24h window vs calendar-day semantics: user-facing message implies discrete reset
  - Missing test for `BudgetExceededError` propagation from `before_iteration` callback
- **Quality**: Clean architecture, good OTEL coverage, comprehensive tests (28), callback pattern in stream.py is well-designed

## Migration Numbering Conflict
Both PR #25 and PR #26 now target migration 0010. Whichever merges second must renumber to 0011.

## Systemic Patterns Observed

1. **Branch staleness**: PR #26 was branched before conversation/ refactoring. PRs that touch core modules risk diverging from main quickly. Consider shorter-lived branches or more frequent rebases.

2. **Broad exception suppression**: `contextlib.suppress(Exception)` used for optional integrations. This hides real bugs. Pattern should be narrowed to specific expected exceptions (ImportError for optional modules).

3. **FOR UPDATE SKIP LOCKED misuse**: The initial implementation used `FOR UPDATE SKIP LOCKED` outside a transaction (autocommit mode), making the lock a no-op. This is a subtle concurrency bug that passes all unit tests since they mock the pool. Integration tests against a real database would catch this.

4. **Migration numbering without main sync**: All three PRs (#25, #26, #30) had migration number collisions. A pre-commit hook or CI check that verifies migration numbers don't collide with main would prevent this class of error. This is now a recurring pattern.

5. **Observable gauges in async contexts**: The intent module tried to create an OTEL observable gauge with a sync callback that couldn't await — resulting in a permanently-zero metric. Async-native codebases need async-compatible metric patterns.

6. **Module-level mutable state for deduplication**: `_warned_bands` in budget.py uses a module-level dict that grows with session IDs. This pattern (process-lifetime mutable state keyed by ephemeral identifiers) will appear in other modules too. Consider establishing a project convention for bounded caches (e.g. `functools.lru_cache` or a simple LRU dict) before this becomes widespread.

7. **Redundant DB queries across budget operations**: The budget module's check→stream→record→warn cycle queries the same aggregates multiple times per turn. This is a general risk when operations are cleanly separated but share data needs. Worth watching as more modules (deliberation, intents) integrate budget checks.
