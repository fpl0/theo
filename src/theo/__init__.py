"""Theo: canonical memory and durable work across native reasoning runtimes.

The import package exposes its version without starting services or loading
adapters. The console command and python -m theo enter the operator CLI.
"""

__version__ = "0.1.0"
