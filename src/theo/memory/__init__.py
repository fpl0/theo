"""Canonical memory storage, retrieval and context projection.

Store owns revision and correction semantics, context selects auditable prompt
evidence, and embeddings maintains optional local semantic indexes.
"""
